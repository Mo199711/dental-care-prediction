# Runtime dependencies (installed by Streamlit Community Cloud).
# Upper bounds are deliberate: they keep the committed model artifacts
# loadable after a major release of scikit-learn or pandas.

pandas>=2.0,<4.0
numpy>=1.24,<3.0
scikit-learn>=1.3,<2.0
xgboost>=2.0,<4.0

matplotlib>=3.7,<4.0
seaborn>=0.12,<1.0

streamlit>=1.28,<2.0

# Transitive pin. Streamlit Community Cloud otherwise resolves a starlette
# release that breaks the app at import time. Remove once upstream is fixed.
starlette<1.4.0
