__pycache__/
*.pyc
.ipynb_checkpoints/
.env
venv/
.venv/
.pytest_cache/

# The trained artifacts in models/ and the seeded dataset in data/ are
# committed on purpose: Streamlit Community Cloud has an ephemeral filesystem
# and would otherwise retrain on every cold start.
